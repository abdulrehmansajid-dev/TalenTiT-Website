import { defineConfig } from 'vite'

export default defineConfig({
  // Using Vite default transforms. If you need React Fast Refresh
  // or additional JSX transforms, install and configure @vitejs/plugin-react.
})
